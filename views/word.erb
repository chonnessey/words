<h4>Word: <%= @word.word %></h4>

<h4>Definition(s)</h4>

  <% if @word.definitions.any? %>
    <ul>
      <% @word.definitions.each do |definition| %>
        <li><a href="/words/<%= @word.id %>/definitions/<%= definition.id %>"><%= definition.definition %></a></li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no definitions for this word yet. Fill out the form below to add some!</p>
  <% end %>

<h4>Add a definition:</h4>

<form action="/words/<%= @word.id %>/definitions" method="post">
  <div class="form-group">
    <label for="def">Definition</label>
    <input id="def" name="def" class="form-control" type="text">
  </div>
  <button type="submit" class="btn-lg btn-success">Add definition</button>
</form>

<p><a href="/words/<%= @word.id %>/edit">Edit word</a></p>