<h1>Words and Definitions!</h1>

<h3>Below are a list of words you can click on and see the definitons for them!</h3>

<% if @words.any? %>
  <ul>
      <% @words.each do |word| %>
    <li><a href="/words/<%= word.id %>"><%= word.word %></a></li>
  <% end %>
  </ul>
<% else %>
  <p>There are no words up yet!</p>
<% end %>

<h1>Add a new Word to the list!</h1>

<form action="/words" method="post">
  <div class="form-group">
    <label for="new_word">Input a word!</label>
    <input id="new_word" name="new_word" class="form-control" type="text">
  </div>
  <button id="add" type="submit" class="btn-lg btn-success">Lets add the word!</button>
</form>

<%# <a href="/words/new">Add a new Word!</a> %>
